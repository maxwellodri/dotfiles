#!/bin/sh
cd "$HOME"
SOCKET="$XDG_RUNTIME_DIR/alacritty.sock"
[ -e "$SOCKET" ] && rm -f "$SOCKET"
pkill -f 'alacritty --daemon' 2>/dev/null || true
sh -c "alacritty --daemon --socket '$SOCKET'; rm -f '$SOCKET'" >/dev/null 2>&1 &
exit 0
