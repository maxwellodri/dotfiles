#!/usr/bin/env bash

while [[ $# -gt 0 ]]; do
  case "$1" in
    --file) FILE="$2"; shift 2 ;;
    --url)  URL="$2";  shift 2 ;;
    *) echo "Unknown option: $1" >&2; exit 1 ;;
  esac
done

if [[ -z "$FILE" ]] || [[ -z "$URL" ]]; then
  echo "Usage: $(basename "$0") --file <file> --url <url>" >&2
  exit 1
fi

until curl -L -C - -o "$FILE" "$URL"; do echo "Retrying..."; sleep 2; done
