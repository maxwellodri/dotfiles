# Options for scripts/bookmark.lua
# Seconds between automatic progress saves (pause/quit also save).
save_period=30
# Key that toggles bookmarking on/off for the current folder (opt-in).
# Default "B" (shift+b). Use "b" at your own risk (conflicts with subtitles).
toggle_key=B
