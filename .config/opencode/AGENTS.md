# Global Rules

## Plans

Plans are ephemeral — kept in conversation context, not written to disk. The planning skill generates them as freeform markdown with two sections (Context + Tasks). The implementation skill reads them from context.
