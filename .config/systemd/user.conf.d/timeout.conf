[Manager]
# Cap stop timeout for user units so a stuck process (e.g. a heavy tmux pane
# in a tmux-spawn-*.scope) can only delay user-session teardown this long,
# instead of the 90s default. See dotfiles commit that added this file.
DefaultTimeoutStopSec=15s
