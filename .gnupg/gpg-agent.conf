pinentry-program /home/maxwell/bin/pinentry-dotfiles
# allow pam_gnupg to pre-seed the GPG passphrase at login (see ~/.config/pam-gnupg
# and /etc/pam.d/system-local-login), so the key is unlocked without a prompt.
allow-preset-passphrase
max-cache-ttl 60480000
default-cache-ttl 60480000
