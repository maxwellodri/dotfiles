# Read by curl only when CURL_HOME points at this dir (the pi wrapper sets
# that, so this applies to agent-spawned curl, not interactive shells).
# Descriptive UA per the crates.io data-access policy; bare "curl/8.x" gets 403.
user-agent = "pi-agent-session/1.0"
